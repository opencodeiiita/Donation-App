import 'package:flutter/material.dart';

class DonationsFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Your Donations Fragment'),
    );
  }
}
